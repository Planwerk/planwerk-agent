{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "DraftResult",
  "description": "Contract for the `draft` --format json output (draft.Result): a captured feature idea drafted into a fileable issue.",
  "type": "object",
  "additionalProperties": false,
  "required": ["title", "description", "motivation", "scope", "body"],
  "properties": {
    "title": { "type": "string" },
    "description": { "type": "string" },
    "motivation": { "type": "string" },
    "scope": {
      "type": "string",
      "enum": ["Small", "Medium", "Large"]
    },
    "body": {
      "description": "The rendered Markdown issue body (house format).",
      "type": "string"
    }
  }
}
